FORM
<%= simple_form_for @room do |form| %>
	
	<%= form.input :name %>
	<%= form.input :address %>
	<%= form.input :description %>
	<%= form.input :number_of_guests %>

	<%= form.button :submit %>
<% end %>